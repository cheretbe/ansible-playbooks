# Generated by Ansible. Any local changes will be overwritten
#
# seaf-daemon answers only the account it runs as, so the query is run as the
# service account through the matching rule in /etc/sudoers.d/zabbix-seafile.
# -H gives seaf-cli that account's HOME; the config directory is passed
# explicitly all the same.
UserParameter=seafile.status,sudo -n -H -u {{ linux_seafile_cli_user }} {{ _linux_seafile_cli_zabbix_status_cmd }}
