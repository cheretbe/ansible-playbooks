# Generated by Ansible at {{ lookup('pipe', 'hostname -f') }}
# Any changes made here will be overwritten

alias_maps = hash:/etc/aliases
append_dot_mydomain = no
biff = no
compatibility_level = 2
inet_interfaces = loopback-only
mailbox_size_limit = 0
mynetworks = 127.0.0.0/8
recipient_delimiter = +
inet_protocols = ipv4

{% if linux_provision_mta_postfix_relayhost %}
relayhost = {{ linux_provision_mta_postfix_relayhost }}
{% else %}
default_transport = error
relay_transport = error
{% endif %}

{# Default is localdomain #}
{% if ansible_facts['domain'] %}
mydomain = {{ ansible_facts['domain'] }}
{% endif %}
{# Default is non-FQDN result from gethostname() plus ".$mydomain" #}
myhostname = {{ ansible_facts['fqdn'] }}
{# Default is $myhostname, localhost.$mydomain, localhost #}
mydestination = $myhostname, {{ ansible_facts['hostname'] }}, localhost.$mydomain, localhost
