# Generated by Ansible at "{{ lookup('pipe', 'hostname -f') }}"
# Any changes made here will be overwritten

# Check if a reboot of the system is necessary to finish upgrade
{{ unanttended_upgrades_cron_reboot_minutes }} {{ unanttended_upgrades_cron_reboot_hours }} * * * root systemd-cat -t ansible-unattended-upgrades /opt/ansible-scripts/unattended_upgrades/check_if_reboot_is_needed.py
